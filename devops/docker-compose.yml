services:
    sic-qa-frontend:
        build:
            context: ..
            dockerfile: devops/Dockerfile
        container_name: sic-qa-frontend
        restart: unless-stopped
        networks:
            - npm_network

networks:
    npm_network:
        external: true
